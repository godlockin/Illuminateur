# Illuminateur 环境变量示例
# 复制此文件为 .env 并填入实际值（仅用于本地开发参考）

# Cloudflare Worker 密钥（通过 wrangler secret put 设置）
ACCESS_TOKEN=your-secure-access-token-here

# AI 服务配置（支持多种提供商）
# 通用配置（推荐使用）
AI_PROVIDER=gemini  # 可选值: gemini, openai
AI_API_KEY=your-ai-api-key-here
AI_MODEL=gemini-2.5-flash  # Gemini: gemini-2.5-flash, gemini-pro; OpenAI: gpt-4, gpt-3.5-turbo
AI_BASE_URL=https://generativelanguage.googleapis.com  # Gemini默认; OpenAI: https://api.openai.com

# 兼容性配置（向后兼容）
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.5-flash

# Cloudflare 资源 ID（在 wrangler.toml 中配置）
D1_DATABASE_ID=your-d1-database-id
R2_BUCKET_NAME=illuminateur-storage

# 应用配置
ENVIRONMENT=development
DEBUG=true

# 获取 API 密钥:
# 
# Gemini API 密钥:
# 1. 访问 https://makersuite.google.com/app/apikey
# 2. 登录 Google 账户
# 3. 创建新的 API 密钥
# 4. 复制密钥并通过 wrangler secret put AI_API_KEY 设置
#
# OpenAI API 密钥:
# 1. 访问 https://platform.openai.com/api-keys
# 2. 登录 OpenAI 账户
# 3. 创建新的 API 密钥
# 4. 复制密钥并通过 wrangler secret put AI_API_KEY 设置
#
# 配置示例:
# 使用 Gemini: AI_PROVIDER=gemini, AI_BASE_URL=https://generativelanguage.googleapis.com
# 使用 OpenAI: AI_PROVIDER=openai, AI_BASE_URL=https://api.openai.com