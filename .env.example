# Illuminateur 环境变量示例
# 复制此文件为 .env 并填入实际值（仅用于本地开发参考）

# Cloudflare Worker 密钥（通过 wrangler secret put 设置）
ACCESS_TOKEN=your-secure-access-token-here
GEMINI_API_KEY=your-gemini-api-key-here

# Cloudflare 资源 ID（在 wrangler.toml 中配置）
D1_DATABASE_ID=your-d1-database-id
R2_BUCKET_NAME=illuminateur-storage

# API 配置
GEMINI_API_BASE_URL=https://generativelanguage.googleapis.com/v1beta
GEMINI_MODEL=gemini-pro

# 应用配置
ENVIRONMENT=development
DEBUG=true

# 获取 Gemini API 密钥:
# 1. 访问 https://makersuite.google.com/app/apikey
# 2. 登录 Google 账户
# 3. 创建新的 API 密钥
# 4. 复制密钥并通过 wrangler secret put GEMINI_API_KEY 设置